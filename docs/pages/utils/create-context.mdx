---
title: createContext
package: '@dwarvesf/react-utils'
description: ''
---

`createContext` is a custom hook used to merge several react refs into a single
one.

## Import

```js
import { createContext } from '@dwarvesf/react-utils'
```

## Return value

The `useClipboard` hook returns an object with the following fields:

| Name        | Type       | Default | Description                              |
| ----------- | ---------- | ------- | ---------------------------------------- |
| `value`     | `string`   |         | The copied value.                        |
| `onCopy`    | `function` |         | Callback function to copy content.       |
| `hasCopied` | `boolean`  | `false` | If `true`, the content has been copied . |

## Import

```js
import { useClipboard } from '@chakra-ui/react'
```

## Usage

```jsx
function Example() {
  const [value, setValue] = React.useState('Hello world')
  const { hasCopied, onCopy } = useClipboard(value)

  return (
    <>
      <Flex mb={2}>
        <Input value={value} isReadOnly placeholder="Welcome" />
        <Button onClick={onCopy} ml={2}>
          {hasCopied ? 'Copied' : 'Copy'}
        </Button>
      </Flex>
      <Editable placeholder="Paste here">
        <EditablePreview width="100%" />
        <EditableInput />
      </Editable>
    </>
  )
}
```

## Parameters

The `createContext()`:

| Name     | Type      | Default | Description |
| -------- | --------- | ------- | ----------- |
| `str`    | `string`  | `_`     |             |
| `max`    | `number`  | `_`     |             |
| `middle` | `boolean` | `false` |             |
